#include "shell.h"
int main() {
    char str1[] = "Hlo, world!";
    char str2[] = "Hello, there!";
    int result;

    result = _strncmp(str1, str2, 6);

    if (result < 0) {
        printf("'%s' is less than '%s' up to 6 characters.\n", str1, str2);
    } else if (result > 0) {
        printf("'%s' is greater than '%s' up to 6 characters.\n", str1, str2);
    } else {
        printf("'%s' is equal to '%s' up to 6 characters.\n", str1, str2);
    }

    return 0;
}
